=== DONE ===

--- 2026-03-13 ---
+ preserve dictionary link when editing list items

Changes in `lib/globals-item-dialog.dart`:
1. Preserve `itemId` during list item edits unless the user changes the name without selecting a dictionary item, which now explicitly switches to manual entry.

--- 2026-03-13 ---
+ block manual -1 place-link entries (use Select Place)

Changes in `lib/globals-item-dialog.dart`:
1. Reject manual "-1" quantity input and require using "Select Place" to create linked lists.
2. Added a guard to prevent saving a place link without a selected place.

--- 2026-03-13 ---
+ fix sort order collisions after reordering unpurchased items

Changes in `lib/list_screen.dart`:
1. Recompute sort_order for the full list after drag-reordering, while preserving purchased item positions.

--- 2026-03-13 ---
+ apply restored theme/language immediately after restore

Changes in `lib/settings_screen.dart`:
1. When restoring with settings enabled, read saved language/theme and apply them before rebuilding the app.

--- 2026-03-13 ---
+ offer move/delete photo choice on swipe-delete

Changes in `lib/list_screen.dart`:
1. When swipe-deleting an item with a photo, show the move/delete dialog and act accordingly.

--- 2026-03-13 ---
+ guard async setState with mounted checks in ListScreen

Changes in `lib/list_screen.dart`:
1. Added `mounted` checks before `setState` after async work in list loading and wakelock settings.

--- 2026-03-13 ---
+ honor onboarding_completed flag for WelcomeScreen

Changes in `lib/main.dart`:
1. Show WelcomeScreen when onboarding is not completed, even if the database exists.

--- 2026-03-13 ---
+ use theme colors in WelcomeScreen

Changes in `lib/welcome_screen.dart`:
1. Use theme colors for the welcome icon and primary button.

--- 2026-03-13 ---
+ refresh Home after restore to show data immediately

Changes in `lib/settings_screen.dart`, `lib/home_screen.dart`, `lib/globals-app-constants.dart`:
1. Added a Home refresh hook and call it after restore, then rebuild/navigate to ensure restored data appears.

--- 2026-03-10 ---
+ replace shopping-specific labels with neutral list terminology

Changes in `assets/locales.json`, `lib/list_screen.dart`, `lib/home_screen.dart`, `lib/settings_screen.dart`, `lib/welcome_screen.dart`, `lib/globals-item-dialog.dart`, `lib/globals-ui-helpers.dart`, `lib/items_dictionary_screen.dart`:
1. Replaced all "товар/товары" with "элемент/элементы" in ru/ua translations.
2. Changed "purchased/unpurchased" to "done/active", "shop mode" to "large font" in all locales.
3. Replaced shopping_cart, store icons with checklist, list_alt, zoom_in.
4. Updated hint examples from "Supermarket, Market" / "Milk, Bread" to neutral ones.

--- 2026-02-23 ---
+ release APK size optimization: enabled compressed native libs (~50% smaller APK)

Changes in `android/app/build.gradle.kts`:
1. Enabled `packaging.jniLibs.useLegacyPackaging = true` for release APK packaging.
2. Native `.so` files are compressed again in APK, which significantly reduced output size.

--- 2026-02-23 ---
+ quality fixes batch: foreign keys, move/copy duplicates, atomic restore, nullable copyWith, swipe photo cleanup, backup comments, onboarding theme sync, globals split/rename

Changes in `database.dart`, `move_items_screen.dart`, `place.dart`, `items.dart`, `list.dart`, `list_screen.dart`, `welcome_screen.dart`, `globals.dart`, `globals-*.dart`, `ToDo.txt`:
1. Enabled SQLite foreign key enforcement and made restore transactional with required-file validation.
2. Fixed duplicate detection for manual items in move/copy and fixed nullable field clearing in model `copyWith`.
3. Fixed swipe-delete to remove attached photos, preserved place comments in backup/restore, and aligned onboarding theme keys with configured themes.
4. Split `globals.dart` into focused `globals-*` modules and updated task/error notes in `ToDo.txt`.

--- 2026-01-23 ---
+ delete PIN-locked places only after entering PIN

Changes in `home_screen.dart`:
1. Function `deletePlace` (line 128-138): added PIN check before delete confirmation dialog
2. Swipe left (Dismissible): now calls `deletePlace` instead of separate logic - ensures:
   - PIN check for locked lists
   - Correct photo handling on delete

Now when trying to delete a PIN-locked list (via menu or swipe), PIN dialog appears first, and only after correct input - delete confirmation dialog.
