===TODO:
+ one universal add/edit list/dict dialog
+ place links (reference to other lists using quantity="-1" and unit="-{placeId}")
? "receipts" (complex items, separate list, example - salads with ingredients)
+ move/copy items between places (lists)
+ make error messages at bottom red and success messages green
+ add clear button to search field in dictionary (<=x)
+ increase font size in lists (dictionary, shopping lists)
+ change dialog button text to "OK"
+ send list to sms,viber etc
+ delete languages list, try compose it
+ add select language
+ add version info and show
+ add color themes
+ change size between strings (dict only)
+ add font size
+ moved strings up/down in lists
+ moved strings up/down in place/list
+ add menu to place/list on long tap to edit, delete
+ add menu to dict on long tap to edit, delete, remove trash icon
+ add edit (right) and delete (left) gestures
+ add backup dbase, csv
+ add restore dbase, csv
+ if add item to dict, change first char to CAPS
+ mark if there are unpurchased items in the list
+ add setting - confirm exit
+ change menu display location
+ add icon
+ add key

===ToFIX:
+ display items in one line in dictionary too ("Milk /L")
+ remove space between quantity and unit (Milk 5L) in lists
+ don't show unit if quantity is not set ("Milk L" -> just "Milk")
+ remove trash icon (delete button) from purchased items
+ display all info in one line in lists: "Milk 5L" instead of title/subtitle
+ make divider between "purchased/unpurchased" in lists more visible, wider
+ purchased items cannot be edited?

===ERRORS:
o дублирование списков (ccc=xx1+xx2+aaa+bbb+aaa ?)
o в AddItem при добавлении списка не показывать отрицательные значения (можно заполнять * ?) и
  сделать "disable" этим полям - запретить редактирование.
o в список нельзя добавлять "родителя" (если доступно)
+ search doesn't work when entering items
+ duplicates are added to dictionary
+ records don't display after restore, rebuild?
+ in dict: if empty, not translated empty message
